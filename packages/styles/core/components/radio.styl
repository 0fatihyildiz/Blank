.blank
    &__radio
        flex(row, center, center, inline-flex)
        cursor pointer
        input
            cursor pointer
            background $BaseBlack
            width 16px
            height 16px
            border-radius 50%
            
            &:checked 
                background: $Info500

        label
            Body2(500)
            color $Primary900
            margin-left 8px
            cursor pointer